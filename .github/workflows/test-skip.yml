# WARNING: DO NOT EDIT!
#
# This file was generated by plugin_template, and is managed by it. Please use
# './plugin-template --github pulp_rpm' to update this file.
#
# For more info visit https://github.com/pulp/plugin_template

---
name: "Test"
on:
  workflow_call:
    inputs:
      matrix_env:
        required: true
        type: string

jobs:
  test:
    runs-on: "ubuntu-latest"
    strategy:
      fail-fast: false
      matrix:
        env: ${{ fromJSON(inputs.matrix_env) }}

    steps:
      - name: "Skip - docs only changes"
        run: echo "Skipping test ${{ matrix.env.TEST }} for docs-only changes"
